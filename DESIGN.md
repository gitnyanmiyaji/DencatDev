# dencat.dev — Design Language Document

このドキュメントは、`dencat.dev` 配下の全ページ・サブサービスが従うべき**構造的・レイアウト的なルール**を定義します。

> **基本方針**：色はページの個性を表す自由な手段とする。ただし「骨格（ナビ・フッター・コンポーネントの配置）」を統一することで、どのページを訪れても「dencat らしさ」が損なわれないようにする。

---

## 1. ページ構造テンプレート

全ページは以下の3層構造を基本とする。

```
┌─────────────────────────────────────────┐
│  <nav>  スティッキーナビゲーション        │  ← 常に最上部に固定
├─────────────────────────────────────────┤
│  <main> メインコンテンツ                  │  ← スクロール領域
│    ・ Hero / ページ固有コンテンツ          │
│    ・ 各セクション                        │
├─────────────────────────────────────────┤
│  <footer> フッター                        │  ← 最下部、固定しない
└─────────────────────────────────────────┘
```

---

## 2. ナビゲーション（`<nav>`）

### 構造
```
[ ● サービス名 ]          [ ホーム  GitHub  dencat.dev ]
  左: ロゴ・サービス名      右: グローバルナビリンク
```

### ルール
- **高さ**: `56px〜64px` の範囲に統一。
- **固定**: `position: sticky; top: 0; z-index: 100`。
- **背景**: ダーク基調のベースカラーに `backdrop-filter: blur(16px〜24px)` をかけたもの。ページの背景色が透過して見えるのが理想。
- **下線**: `1px solid` のサブカラー（ページアクセントカラーの低透明度）で区切り線を入れる。
- **ロゴ**: 左寄せ。サービス名はページのアクセントカラーで表示し、前に小さなインジケーター（ドット等）を置くことができる。
- **リンク**: 右寄せ。`0.8rem` 前後、`letter-spacing` ありで落ち着いた見た目にする。

### グローバルに含めるリンク（推奨）
| リンク名 | 遷移先 |
|---|---|
| ホーム | `/` or `https://dencat.dev` |
| GitHub | `https://github.com/gitnyanmiyaji` |
| dencat.dev | `https://dencat.dev` |

---

## 3. メインコンテンツ（`<main>`）

- **最大幅**: `960px〜1100px` で中央寄せ。
- **パディング**: 左右 `24px`、下部 `80px` 程度。
- **Zインデックス**: 背景エフェクトより前面（`z-index: 1` 以上）。

### セクション区切り
セクションの見出しは以下のパターンに統一する。

```html
<div class="section-header">
  <h2>セクション名</h2>
</div>
```

- `h2` は小さめのキャップス文字（`0.7rem`、`letter-spacing: 0.15em`、`text-transform: uppercase`）。
- 見出しの後ろに `::after` で横線（`1px solid` の境界線）を引く。

---

## 4. 背景

- **ベースカラー**: `#05070a` 周辺の極暗ネイビー〜ブラック。ページによって `#030507`〜`#080c14` の範囲で調整可。
- **グラデーション（任意）**: `radial-gradient` で中心をわずかに明るくするのは可。
- **テクスチャ（任意）**: 薄いグリッド線（`opacity: 0.03〜0.05`）やパーティクルエフェクトはページの個性として許可。
- **禁止**: 白や明るい背景は使用しない。ライトモード対応は現時点でスコープ外。

---

## 5. タイポグラフィ

| 用途 | フォント | 備考 |
|---|---|---|
| 欧文・UI全般 | `Outfit` (Google Fonts) | 300 / 400 / 600 / 700 |
| 日本語テキスト | `Noto Sans JP` (Google Fonts) | 300 / 400 / 500 |
| コード・URL | `JetBrains Mono` (Google Fonts) | 400 / 500 |

```html
<!-- 全ページ共通のフォント読み込み -->
<link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&family=Noto+Sans+JP:wght@300;400;500;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet" />
```

### テキストカラー
ページのアクセントカラーは自由だが、本文テキストは以下の3階層で統一する。

| レイヤー | 用途 | 明度の目安 |
|---|---|---|
| Primary | 見出し・強調 | 明るい（`#e8edf5` 周辺） |
| Secondary | 本文・説明文 | 中程度（`#8a9bb5` 周辺） |
| Muted | URL・補助情報 | 暗め（`#4a5878` 周辺） |

---

## 6. コンポーネントパターン

### 6.1 グラスカード
情報の塊を表現する基本ユニット。

```css
.card {
  background: rgba(10, 16, 30, 0.6〜0.8);
  backdrop-filter: blur(12px〜20px);
  border: 1px solid rgba(アクセントカラー, 0.15〜0.25);
  border-radius: 12px〜16px;
  /* ホバー時にボーダーを強調し、わずかに浮かせる */
  transition: border-color 0.25s, box-shadow 0.25s, transform 0.2s;
}
.card:hover {
  border-color: rgba(アクセントカラー, 0.5〜0.6);
  box-shadow: 0 0 24px〜32px rgba(アクセントカラー, 0.08〜0.12);
  transform: translateY(-2px);
}
```

### 6.2 コールアウトボックス
ユーザーへの重要なアクションを促すボックス。

- `border-radius: 14px〜16px`
- `border: 1px solid rgba(アクセントカラー, 0.4〜0.6)`（通常カードより目立たせる）
- `box-shadow` でアクセントカラーのグローを追加。
- タイトルはアクセントカラーで `0.85rem`、`letter-spacing`あり。

### 6.3 バッジ・タグ
```css
.tag {
  font-size: 0.65rem;
  font-weight: 600;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: アクセントカラー;
  background: rgba(アクセントカラー, 0.12〜0.18);
  border: 1px solid rgba(アクセントカラー, 0.2);
  border-radius: 100px;
  padding: 3px 10px;
}
```

---

## 7. フッター（`<footer>`）

### 構造・ルール
- **配置**: ページ最下部。`position: fixed` は使わない（スクロールで到達する）。
- **区切り**: 上部に `1px solid` のサブカラーボーダー。
- **テキスト**: `text-align: center`、小さめ（`0.75rem〜0.8rem`）、ミュートカラー。
- **必須要素**:
  - ライセンス表記（MIT License 等）
  - GitHub リンク
  - `— dencat` のシグネチャ（イタリック、やや明るいカラー）

```html
<!-- フッターの最小テンプレート -->
<footer>
  <p>MIT License · <a href="https://github.com/gitnyanmiyaji">GitHub</a></p>
  <p class="sig">— dencat</p>
</footer>
```

---

## 8. アクセントカラーの自由度

各サービス・ページは独自のアクセントカラーを持つことができる。ただし以下を守ること。

| 制約 | 理由 |
|---|---|
| 背景は必ずダーク系を維持する | ライトモード・明るい背景は世界観を壊す |
| アクセントカラーは彩度・明度をある程度保つ | くすみすぎると視認性と個性が失われる |
| 1ページに主要アクセントカラーは原則1色 | ごちゃごちゃした印象を防ぐ |

### 既存ページのアクセントカラー例

| ページ | アクセントカラー | 印象 |
|---|---|---|
| `dencat.dev` | `#88aaff`（ラベンダーブルー） | ポータル、穏やか |
| `skills.dencat.dev` | `#3ab4ff`（アイスブルー） | 技術的、クリア |

---

## 9. 新規ページを作るときのチェックリスト

- [ ] フォント3種（Outfit / Noto Sans JP / JetBrains Mono）を読み込んでいる
- [ ] ナビゲーションが sticky で左ロゴ・右リンクの構造になっている
- [ ] dencat.dev へのグローバルリンクがある
- [ ] 背景はダーク系（`#05070a` 周辺）である
- [ ] カード・コールアウトに backdrop-filter blur が当たっている
- [ ] フッターに `— dencat` シグネチャがある
- [ ] セクション区切りが `section-header` パターンに準じている

---

*このドキュメントは dencat.dev エコシステムの拡張とともに随時更新する。*  
**— dencat**
